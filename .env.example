# Copy this file to .env.local and adjust values for your setup.

# Backend identity server URL (required).
# This is the only server the frontend connects to.
NEXT_PUBLIC_BACKEND_URL=http://localhost:8080

# Moderation server URL (optional).
# Set this if you are running the moderation panel.
NEXT_PUBLIC_MODERATION_URL=http://localhost:8090
